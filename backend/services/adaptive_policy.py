def decide_next_step(score_percent: int, attempt_no: int) -> str:
    if score >= 70:
        NEXT_LESSON
    elif quiz_type == "mixed":
       GO_SIMPLIFIED_QUIZ
    else:
        SHOW_SUPPORT_OPTIONS